---
all:
  children:

    splunk_uf: # Note that you can nest additional groups under here to use with group_vars, e.g. to assign clientName - see production inventory file for more examples
      hosts:
        my_first_uf:
        my_second_uf:

    splunk_full:
      children:
        splunk_hf:
          hosts:
            some_hf:

        splunk_search:
          hosts:
            my-dev-host:
