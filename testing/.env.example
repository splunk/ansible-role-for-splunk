# Optional environment overrides for the local testing harness.
#
# This file is intentionally minimal; most configuration is driven by `MOLECULE_ENV`
# (set in your shell) and by generated files under `testing/.secrets/`.
#
# Example:
#   MOLECULE_ENV=prod task infra:test
