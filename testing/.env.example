# Twingate Configuration (Optional)
# Uncomment and set your Twingate network and tokens for external access
# If not set, Twingate connector will not be started
# TWINGATE_NETWORK=your-network-name
# TWINGATE_ACCESS_TOKEN=your-access-token
# TWINGATE_REFRESH_TOKEN=your-refresh-token