---
# Simple test inventory for day0 - just 2 Splunk nodes
all:
  vars:
    ansible_user: ansible
  children:
    full:
      hosts:
        splunk-license:
          # License master + DMC + Search Head
        splunkapp-prod01:
          # Single indexer (no clustering)

    search:
      hosts:
        splunk-license:  # License server also acts as search head

    ansible_controller:
      hosts:
        ansible-controller:
          ansible_user: ansible